\copy (SELECT DISTINCT d.project_id, p.repo_id, d.dep_id, d.project, '' AS repo_name, d.dep_name, FROM dependencies d JOIN projects p ON d.project_id = p.project_id WHERE lower(d.dep_kind) similar to 'runtime%|%provide%|["provided"]|%compile%|["compile"]|imports|["runtime"]|peer|depends|system|import|["system"]|["import"]|external|["external"]' order by d.project_id, d.dep_id) to './project-runtime_deps.csv' csv header
